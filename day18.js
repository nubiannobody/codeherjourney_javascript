/* Can you name the parameters within this function?
BONUS: What does the following code print to the console? */

function chairCount(rows, columns) {
    return rows * columns;
}

const venueCapacity = chairCount(5, 4);

console.log(venueCapacity); // 20